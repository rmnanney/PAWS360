package com.uwm.paws360.config;

/**
 * Placeholder SecurityConfig kept in main sources temporarily.
 * Previously moved to test sources; this no-op file prevents accidental use
 * of Spring Security compile-time classes in the production classpath.
 */
public class SecurityConfig {
    // No-op placeholder. Test-specific configuration is available under src/test/java.
}