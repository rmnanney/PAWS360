FROM quay.io/coreos/etcd:v3.5.11

# The official etcd image is built from scratch with no shell
# We cannot add packages or modify it
# All configuration is done via environment variables

EXPOSE 2379 2380

# Use the default etcd entrypoint from the base image
